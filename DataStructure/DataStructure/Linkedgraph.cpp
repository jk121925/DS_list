#include "graphlinkedlist.h"
#include <stdio.h>
#include <string.h>
#include <vector>
/*
LinkedGraph::LinkedGraph(int maxVertexCount) {
	int i = 0, j = 0;

	this->graphType = 1; //unconnected
	this->maxVertexCount = maxVertexCount;
	this->currentVertexCount = 0;
	this->currentEdgeCount = 0;
	
	this->pVertex = new int[maxVertexCount];
	memset(this->pVertex, 0, sizeof(int) * maxVertexCount);
	this->ppAdjEdge = new <vector>* [maxVertexCount];
	for (i = 0; i < maxVertexCount; i++) {
		this->ppAdjEdge[i] = 
	}
}

int main() {
	return 0;
}
*/